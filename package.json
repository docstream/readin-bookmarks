{
  "name": "ds3g-bookmarks",
  "version": "1.0.4",
  "main": "./lib/bookmark.js",
  "scripts": {
    "pretest": "node_modules/.bin/coffee --bare --compile --output lib/ src/",
    "postinstall": "node_modules/.bin/coffee --bare --compile --output lib/ src/",
    "compile": "node_modules/.bin/coffee --bare --compile --output lib/ src/",
    "test": "PATH=./node_modules/.bin:$PATH grunt"
  },
  "dependencies": {
    "mongoose": "4.3.6",
    "underscore": "1.8.3"
  },
  "devDependencies": {
    "coffee-script": "1.8.0",
    "should": "~1.3.0",
    "lodash": "2.4.1",
    "proxyquire": "0.6.0",
    "grunt": "~0.4.1",
    "grunt-cli": "0.1.13",
    "grunt-contrib-clean": "^0.5.0",
    "grunt-contrib-watch": "~0.6.1",
    "grunt-env": "^0.4.1",
    "grunt-istanbul": "^0.2.5",
    "grunt-mocha-test": "^0.10.2",
    "grunt-bump": "~0.1.0"
  }
}
